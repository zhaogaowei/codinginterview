#include <iostream>
#include <cstdio>

using namespace std;

int main() {
	//while (true)
	//{
	//	int len;
	//	int l[2],n[2];
	//	cin >> len;
	//	int result = 0;
	//	vector<int> v(len+1,0);
	//	/*cin >> l[0] >> n[0] >> l[1] >> n[1];
	//	for (int i = 0; i < 2; i++)
		/*{
			for (int j = 1; j <= len; j++)
			{
				if (j + l[i] == len)
					v[j] = v[j - 1] + 1;
				else
					v[j] = v[j - 1];
			}
		}
		cout << v[len - 1];
	}*/
	int b = 1e9 + 7 > INT_MAX ? 1 : 0;
	cout << b << endl;
	return 0;
}